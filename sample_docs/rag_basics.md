# RAG 基础

RAG 是 Retrieval-Augmented Generation 的缩写，核心思路是先检索外部知识，再将相关内容提供给大模型生成答案。

RAG 常见流程包括：
1. 文档切分；
2. 向量化；
3. 向量检索；
4. 可选 rerank；
5. 拼接上下文；
6. 生成答案。

rerank 的作用是：在初步召回后，对候选文档重新排序，提高最终送入大模型的上下文相关性。